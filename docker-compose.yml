version: '3.8'

services:
  mcp-server:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - MONGO_URI=${MONGO_URI:-mongodb://localhost:27017}
      - DB_NAME=${DB_NAME:-dreamlodge}
      - PORT=${PORT:-8080}
    restart: always